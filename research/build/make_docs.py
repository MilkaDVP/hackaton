# -*- coding: utf-8 -*-
"""Собирает research/REPORT.md и research/slide.md из results.json.

Запуск из папки research/build: python make_docs.py
REPORT.md — отчёт по исследованию; README.md в корне описывает продукт.

Так во всех документах стоят ровно те числа, что посчитаны в ноутбуке.
"""
import json

R = json.load(open("../results.json", encoding="utf-8"))

L1, L2 = R["level1"], R["level2"]
B1, B2 = R["baseline1"], R["baseline2"]
OV = R["overlap"]
top1, top2, top3 = R["top3"]


def pm(d, key="roc"):
    return f"{d[key]:.3f} ± {d[key + '_sd']:.3f}"


def transfer_row(level, tag_start):
    for r in R["transfer"]:
        if r["уровень"] == level and r["схема"].startswith(tag_start):
            return r
    return None


t_naive_l2 = transfer_row("L2", "НАИВНО")
t_fair_l2 = transfer_row("L2", "ЧЕСТНО")
t_naive_l1 = transfer_row("L1", "НАИВНО")
t_fair_l1 = transfer_row("L1", "ЧЕСТНО")

d_roc2 = L2["roc"] - B2["roc"]
d_pr2 = L2["pr"] - B2["pr"]
d_roc1 = L1["roc"] - B1["roc"]
d_pr1 = L1["pr"] - B1["pr"]

NS = R["nested2"]
DIR_ROWS = "\n".join(
    f"| {i} | **{d['фактор']}** | {d['направление']} |"
    for i, d in enumerate(R["top3_dir"], 1))

SLIDE_DIRS = "\n".join(
    f"{i}. **{d['фактор']}** — {d['направление'].split('(')[0].strip()}"
    for i, d in enumerate(R["top3_dir"], 1))

goal = R["goal_met"]
stretch = ("достигнута" if goal["L2_stretch_086"] else
           f"**не достигнута** (получено {L2['roc']:.3f})")

readme = f"""# Кто не получит зачёт — решение кейса

Модель предсказывает риск незачёта (`G3 < 10`) по анкете и учебному поведению студента
и выдаёт куратору короткий список тех, с кем стоит поговорить.

Данные: `student-por.csv` — {R['n_students']} студентов, незачётов {R['n_fail']}
({R['fail_rate']:.1%}). Разделитель `;`, целевая переменная делается из `G3`.

## Финальные метрики

Все числа — `среднее ± стандартное отклонение` по `RepeatedStratifiedKFold`
(5 фолдов × 5 повторов = 25 оценок). Baseline — подход стартового ноутбука
(`get_dummies` + случайный лес), пересчитанный на том же протоколе.

| Уровень | Метрика | Baseline стартера | Наша модель | Ориентир ТЗ |
|---|---|---|---|---|
| **1 — с `G1`, `G2`** | ROC-AUC | {pm(B1)} | **{pm(L1)}** | ≈ 0.97 |
| | PR-AUC | {pm(B1, 'pr')} | **{pm(L1, 'pr')}** | — |
| **2 — без `G1`, `G2`** | ROC-AUC | {pm(B2)} | **{pm(L2)}** | ≈ 0.83 |
| | PR-AUC | {pm(B2, 'pr')} | **{pm(L2, 'pr')}** | — |

Прирост к baseline: уровень 1 — ROC-AUC {d_roc1:+.4f}, PR-AUC {d_pr1:+.4f};
уровень 2 — ROC-AUC {d_roc2:+.4f}, PR-AUC {d_pr2:+.4f}.

Прирост проверен парным сравнением по одним и тем же фолдам: на уровне 2 наша модель
выиграла в {int(R['paired']['L2']['wins'])} из {int(R['paired']['L2']['n'])} фолдов,
p (парный t-тест) = {R['paired']['L2']['p_t']:.2g}.

Базовый уровень PR-AUC для случайной модели равен доле положительного класса —
{R['fail_rate']:.3f}, а не 0.5.

### Честная оценка без оптимизма

Параметры моделей когда-то подбирались на этих же данных, поэтому число выше
слегка оптимистично. **Вложенная кросс-валидация всей процедуры** («подобрать параметры
трёх членов → собрать ансамбль», весь подбор внутри каждого внешнего фолда) даёт
для уровня 2: **ROC-AUC {NS['roc']:.3f} ± {NS['roc_sd']:.3f}**, PR-AUC {NS['pr']:.3f} ± {NS['pr_sd']:.3f}.

Разница между 5×5 CV и вложенной CV ({L2['roc'] - NS['roc']:+.3f}) — это и есть цена
самообмана при подборе. Оба числа названы явно; на новом потоке студентов стоит
ожидать второе.

### Что достигнуто, а что нет

* Ориентир ТЗ для уровня 1 (≈ 0.97): **{'достигнут' if goal['L1'] else 'не достигнут'}**.
* Ориентир ТЗ для уровня 2 (≈ 0.83): **{'достигнут' if goal['L2'] else 'не достигнут'}**.
* Повышенная планка ROC-AUC ≥ 0.86 на уровне 2: {stretch}.
  Дальнейший рост упирается в объём выборки — 649 строк и 100 незачётов;
  см. кривую обучения в разделе 11.1. Поднять число можно было бы подбором
  по той же кросс-валидации, по которой мы отчитываемся, но это была бы подгонка,
  а не качество, и вложенная CV её бы вскрыла.

**Уровень 2 — главный.** Он работает до первой контрольной, когда у куратора ещё есть
время что-то сделать. Уровень 1 показан как верхняя граница: он использует оценки
за две контрольные, то есть предсказывает оценку по оценкам.

## Три главных фактора (уровень 2, до первой контрольной)

| # | Фактор | Направление влияния (посчитано в разделе 9.8) |
|---|---|---|
{DIR_ROWS}

Почему это не случайность конкретного запуска — проверено по трём осям (раздел 9):

* **3 разных `random_state`** — тройка не меняется;
* **3 семейства моделей** (ExtraTrees, логрегрессия L1, HistGB) — тройка не меняется;
* **2 независимых метода измерения** (permutation importance и drop-column importance,
  плюс SHAP и коэффициенты логрегрессии для направления) — консенсус рангов;
* **бутстреп по трём разбиениям** — доверительные интервалы величины вклада (см. оговорку ниже).

Важность считается по **исходным переменным**, а не по колонкам после кодирования:
инженерия признаков живёт внутри пайплайна, поэтому перемешивание `absences` ломает
и все производные от неё признаки, и измеряется полный вклад переменной.

**Оговорка про доверительные интервалы.** Порядок факторов устойчив (одна и та же
тройка в 9 запусках из 9), но бутстреп-интервалы величины вклада широкие и у части
факторов накрывают ноль — выборка мала. Мы защищаем **ранжирование** факторов,
а не конкретные «проценты влияния».

⚠️ **`school` — самый неудобный из трёх.** Это не поведение студента, а то, в какой
из двух школ он учится (разрыв в доле незачётов между ними велик — см. раздел 9.9).
Куратору внутри одной школы он бесполезен как признак: он у всех одинаковый.
Практический смысл ровно один — **порог надо считать отдельно по каждой школе**.
И почти наверняка за ним стоит не «школа как таковая», а состав студентов и район.
Цена отказа от этого признака посчитана в разделе 9.9.

⚠️ **Корреляция — не причинность.** `failures` — симптом, а не рычаг.
Модель говорит «студенты с такой историей чаще не сдают», а не «уберите
прошлые незачёты, и он сдаст».

## Выбранный порог

**Порог {R['threshold']}, что соответствует списку из {R['list_size']} студентов**
(ёмкость куратора K = {R['capacity']}).

Обоснование — ёмкость, а не абстрактная «цена ошибки»: заказчик не может назвать,
во сколько раз пропуск дороже ложной тревоги, зато точно знает, со сколькими студентами
куратор успеет поговорить. Порог, привязанный к ёмкости, ещё и саморегулируется:
если риск на потоке вырастет, список останется той же длины.

На выбранном пороге:

* поймано **{R['caught']}** студентов из {R['n_fail']} незачётов;
* зря позвано **{R['false_alarm']}**;
* пропущено **{R['missed']}**;
* список в **{R['lift']:.1f} раза** «гуще» незачётами, чем случайная выборка того же размера.

Порог 0.5 «по умолчанию» для этой задачи не работает: при {R['fail_rate']:.1%}
положительного класса он оставляет большинство студентов в зоне риска без внимания
(раздел 8.4 ноутбука).

Вероятности **откалиброваны** (изотоническая регрессия внутри пайплайна) — иначе
порог не имеет содержательного смысла. Калибровка не меняет ROC-AUC, но чинит
интерпретацию «0.3 = примерно 30 из 100 таких студентов не сдадут».

## Перенос на математику и ловушка «382 студентов»

`pd.merge(por, mat)` по демографическим колонкам даёт **{OV['merge_rows']}** строк —
и это число обычно принимают за количество общих студентов. Это неверно:
ключ не уникален, и мёрдж строит декартово произведение.

* общих уникальных ключей: **{OV['common_keys']}**;
* строк por в пересечении: {OV['por_rows']}, строк mat: {OV['mat_rows']};
* непересекающаяся часть математики: **всего {OV['mat_clean']} человек**.

Поэтому «строгая» оценка переноса (обучение на чистом por → тест на чистом mat)
имеет огромную погрешность, и мы приводим три схемы сразу:

| Уровень | Наивно (весь por → весь mat) | Честно (por без пересечения → весь mat) | Потолок (CV внутри mat) |
|---|---|---|---|
| 1 — с оценками | ROC-AUC {t_naive_l1['ROC-AUC']:.3f} | {t_fair_l1['ROC-AUC']:.3f} | {R['ceiling']['L1']:.3f} |
| 2 — без оценок | ROC-AUC {t_naive_l2['ROC-AUC']:.3f} | {t_fair_l2['ROC-AUC']:.3f} | {R['ceiling']['L2']:.3f} |

Три вывода:

1. Разрыв между наивной и честной оценкой **невелик** — модель на 649 строках
   с регуляризацией не запоминает отдельных людей. Но утверждать это можно только
   потому, что посчитаны оба варианта.
2. Модель с оценками переносится почти без потерь, без оценок — плохо.
3. Главное: сравнение с потолком показывает, что дело **не в переносе между предметами**.
   На математике сигнала без оценок мало в принципе. Значит, «три главных фактора» —
   утверждение про этот поток и этот предмет, а не универсальный закон.

Дополнительно: для {OV['paired_students']} студентов, сопоставленных однозначно,
корреляция итоговых баллов по двум предметам {OV['corr_g3']:.3f} — «двойка» лишь отчасти
свойство человека, в основном это свойство пары «человек–предмет».

## Как запустить

```bash
python -m venv .venv
.venv/Scripts/activate          # Windows;  source .venv/bin/activate — Linux/macOS
pip install -r requirements.txt

jupyter nbconvert --to notebook --execute \\
    --ExecutePreprocessor.timeout=1800 solution.ipynb
```

Ноутбук выполняется от начала до конца на чистом ядре примерно за
**{R['runtime_sec'] / 60:.0f} минут**, все `random_state` зафиксированы.

Опциональные библиотеки (`lightgbm`, `xgboost`, `catboost`, `shap`, `imbalanced-learn`)
подключаются через `try/except`: если их нет и нет интернета, ноутбук не падает,
а откатывается на `HistGradientBoostingClassifier` и permutation importance.

Тяжёлый перебор гиперпараметров вынесен под флаг `RUN_HEAVY_SEARCH = False`.
При `False` вложенная кросс-валидация всё равно считается — в облегчённом варианте,
поэтому чисел, вписанных руками, в ноутбуке нет.

`REPORT.md` и `slide.md` генерируются скриптом `build/make_docs.py` из `results.json`,
который выгружает сам ноутбук, — чтобы во всех документах стояли одни и те же числа.

## Что в ноутбуке

| Раздел | Содержание |
|---|---|
| 0–1 | Загрузка, версии, целевая переменная, разбор `G3 == 0` |
| 2 | EDA: распределения, boxplot, корреляции, мультиколлинеарность |
| 3 | Утечки: что нельзя пускать в признаки и почему |
| 4 | Признаки: инженерия, таблица «до / после», leave-one-out |
| 5 | Модели, ансамбли, честный протокол (nested CV), решение по `G3 == 0` |
| 6–7 | Уровень 1 и уровень 2: финальные метрики, ROC/PR-кривые, значимость прироста |
| 8 | Калибровка и выбор порога: цена ошибки против ёмкости куратора |
| 9 | Три фактора: 4 метода, 3 оси устойчивости, бутстреп-интервалы |
| 10 | Перенос на математику и ловушка «382 студентов» |
| 11 | Собственные исследования: кривая обучения, тайминг, справедливость, ошибки |
| 12 | Итог для куратора без жаргона + этика и последствия |
| 13 | Что пробовали и что не сработало |

## Ограничения

1. Две школы, один учебный год, один предмет, {R['n_students']} студентов.
2. Порог подобран на этих же данных; на новом потоке его надо пересчитать.
3. Модель без оценок слабо переносится на другой предмет (см. выше).
4. Часть признаков — самооценочные анкетные (алкоголь, здоровье, свободное время).
5. Причинность не установлена; фактор — повод спросить, а не инструкция, что чинить.
6. В признаках есть `sex`, `address`, образование и работа родителей. Модель,
   распределяющая внимание куратора по этим признакам, — осознанное решение,
   которое заказчик должен быть готов защищать. Проверка на перекос — раздел 11.4.
"""

with open("../REPORT.md", "w", encoding="utf-8") as f:
    f.write(readme)

# --------------------------------------------------------------------------
slide = f"""# Кто не получит зачёт
### Пять минут: задача, решение, ограничения

---

## Задача

У куратора {R['n_students']} студентов. К концу семестра **{R['n_fail']}** из них
не получат зачёт — это {R['fail_rate']:.0%}.

Вопрос заказчика: **с кем поговорить, пока не поздно?**
И второй: **на что вообще смотреть?**

---

## Почему не «точность»

Модель, которая всем подряд ставит «зачёт», будет права в {1 - R['fail_rate']:.0%} случаев
и не найдёт **ни одного** студента в зоне риска.

Поэтому мы меряем:

* **ROC-AUC** — умеет ли модель ставить рискующих выше благополучных;
* **PR-AUC** — насколько чистым получится список куратора.

И всегда — с **разбросом** по 25 разбиениям данных, а не одним числом.

---

## Два момента семестра

| Когда | Что знаем | Качество | Польза |
|---|---|---|---|
| **До первой контрольной** | анкета, пропуски, прошлые незачёты | ROC-AUC **{L2['roc']:.3f} ± {L2['roc_sd']:.3f}** | есть время помочь |
| **Конец семестра** | плюс две контрольные | ROC-AUC **{L1['roc']:.3f} ± {L1['roc_sd']:.3f}** | помогать поздно |

Вторая модель почти идеальна — и почти бесполезна:
она предсказывает оценку по оценкам.

**Продукт — первая.**

<small>Числа — по 25 разбиениям данных. Строгая проверка «с нуля»
(вложенная кросс-валидация) даёт для первой модели {NS['roc']:.3f} ± {NS['roc_sd']:.3f} —
именно этого стоит ждать на новом потоке.</small>

---

## Три главных фактора

до первой контрольной:

{SLIDE_DIRS}

Проверено, что это не случайность одного запуска:
3 разных разбиения данных × 3 разных типа модели × 2 независимых способа измерить
важность. Одна и та же тройка вышла наверх в **9 запусках из 9**.

<small>Устойчив именно **порядок** факторов. Насколько сильно влияет каждый —
на 649 студентах сказать точно нельзя, доверительные интервалы широкие.
Поэтому «процентов влияния» мы не обещаем.</small>

---

## Но это симптомы, а не рычаги

Модель говорит: «студенты с такими характеристиками чаще не сдают».

Она **не** говорит: «уберите причину — и он сдаст».

Прошлые незачёты — след той же проблемы, которая приведёт и к новому незачёту.
Лечить надо не термометр.

А «учебное заведение» — вообще не свойство студента. Внутри одной школы
он у всех одинаковый и не помогает выбрать, кого звать. Его смысл другой:
**порог надо считать отдельно по каждой школе.**

**Фактор — это повод спросить, а не инструкция, что чинить.**

---

## Список, а не вероятность

Куратор успевает поговорить с **{R['capacity']}** студентами.
Значит, вопрос не «какой порог», а «кто эти {R['capacity']}».

Из списка в **{R['list_size']}** человек:

* **{R['caught']}** действительно в зоне риска
* **{R['false_alarm']}** позваны зря
* **{R['missed']}** пропущены

Список **в {R['lift']:.1f} раза гуще**, чем если брать наугад.

Порог «0.5 по умолчанию» здесь не работает вообще.

---

## Ловушка, которую легко не заметить

Рядом лежит второй файл — математика. Обычная проверка «обучил на одном,
проверил на другом» частично тестируется **на тех же людях**.

Стандартный способ их найти даёт **{OV['merge_rows']}** совпадений.
Мы проверили — это **неправда**: реально общих студентов **{OV['common_keys']}**,
а {OV['merge_rows']} — артефакт склейки по неуникальному ключу.

Непересекающихся студентов в математике остаётся **{OV['mat_clean']}** —
слишком мало, чтобы им верить.

---

## Что показал честный перенос

| | Наивно | Честно | Потолок |
|---|---|---|---|
| с оценками | {t_naive_l1['ROC-AUC']:.3f} | {t_fair_l1['ROC-AUC']:.3f} | {R['ceiling']['L1']:.3f} |
| без оценок | {t_naive_l2['ROC-AUC']:.3f} | {t_fair_l2['ROC-AUC']:.3f} | {R['ceiling']['L2']:.3f} |

«Потолок» — это если учить модель **прямо на математике**.

Без оценок она слаба даже там. Значит, дело не в том, что мы учились
на другом предмете, — сигнала мало в принципе.

**Наши три фактора — про этот поток, а не универсальный закон.**

---

## Если модель ошиблась

**Позвали зря** — потеряли полчаса. Хуже другое: ярлык.
Если студент решит, что его «записали в отстающие», прогноз может сбыться
именно потому, что был сделан.

**Пропустили** — таких большинство. Система **дополняет** внимание куратора,
а не заменяет его. Не попал в список ≠ «всё хорошо».

Список не показываем студенту. Разговор — поддерживающий.

---

## Ограничения — коротко

* две школы, один год, один предмет, {R['n_students']} человек
* порог привязан к ёмкости куратора; на новом потоке пересчитать
* часть данных — самооценка студента (алкоголь, здоровье, свободное время)
* в признаках есть пол, город/село, образование родителей —
  это осознанное решение, проверка на перекос сделана
* **причинность не установлена**

---

## Итог

Инструмент, который в начале семестра отдаёт куратору
список из {R['list_size']} человек, где в **{R['lift']:.1f} раза** больше
студентов в зоне риска, чем при отборе наугад.

Три вещи, на которые смотреть: **{top1}**, **{top2}**, **{top3}**.

И честная оговорка: это повод поговорить, а не приговор.
"""

with open("../slide.md", "w", encoding="utf-8") as f:
    f.write(slide)

print("REPORT.md и slide.md собраны из results.json")
