# Версии зафиксированы под проверенное окружение (Python 3.11.9).
# Расчётное ядро
numpy==2.4.6
pandas==3.0.5
scikit-learn==1.9.0
scipy==1.17.1
joblib==1.6.0

# Графики
matplotlib==3.11.1

# Запуск ноутбука
ipykernel==7.3.0
nbformat==5.11.1
nbconvert==7.17.1

# --- НЕОБЯЗАТЕЛЬНЫЕ ---
# Нужны только блоку 9 «Какие модели пробовали». Без них ноутбук проходит
# целиком: импорты обёрнуты в try/except, а в таблице сравнения у этих
# строк стоит «не установлено». В итоговое решение ни одна из них не входит —
# все три проиграли случайному лесу. Ставить: pip install -r requirements-optional.txt
# lightgbm==4.7.0
# xgboost==3.2.0
# catboost==1.2.10
