# Проект: предсказание незачёта (хакатон, кейс 6)

## Данные
student-por.csv (649×33), student-mat.csv (395×33), разделитель `;`.
Целевая: y = (G3 < 10). Дисбаланс 15.4 % на португальском.

## Выбор модели (блок 9, проверено)
Семейство выбрано замером: 10 моделей на одних разбиениях.
ExtraTrees 0.832, лес из коробки 0.826, XGBoost 0.811, LightGBM рег. 0.809,
CatBoost 0.798, LightGBM коробка 0.793, HistGB 0.788, логрегрессия 0.788,
MLP(32) 0.701, MLP(64,32) 0.638. Настроенный лес обгоняет лучший бустинг
на 0.028, выигрывая 18 фолдов из 20. XGBoost четвёртым в смесь: −0.003 / −0.002.
lightgbm, xgboost, catboost — необязательные, импорт через try/except.

## Незыблемые правила
- G3 никогда не в признаках. Ассерт обязателен.
- Рабочий уровень — без G1/G2: список нужен в середине семестра.
- Все метрики с разбросом по фолдам. Accuracy не использовать.
- Сравнение вариантов — только парное, на одних и тех же фолдах.
- Отбор на фолдах seed=0, подтверждение на seed=1.
- Permutation importance только out-of-fold.
- Только scikit-learn, все random_state = 42.

## Эталон (измерено, блок 13 — фолды seed=1, n_repeats=6)
Уровень 1: ROC-AUC 0.966 ± 0.010. Уровень 2: baseline 0.830 ± 0.032,
настроенный лес 0.836 ± 0.035, финальная смесь 0.843 ± 0.036.
Перенос на математику без G1/G2 = 0.631. AUC > 0.99 на уровне 2 = утечка.

Победитель сетки: `max_features=0.3, min_samples_leaf=10, max_depth=None`.
Верхняя десятка сетки лежит в 0.8370–0.8387 — плато, конкретный победитель
не определяется, надёжно только направление (крупный лист).
Веса смеси: 0.4 / 0.3 / 0.3, выбраны тайбрейком по числу выигранных фолдов
среди четырёх комбинаций в пределах 0.001 от лучшей.

## Три фактора (уровень 2, permutation importance out-of-fold)
1. school=MS — 30 % незачётов против 7.6 % в GP; в тройке в 100 % замеров.
   Не переносится: в математике 37 % против 32 %, разницы почти нет.
2. failures — 9 % при нуле, 46 % при одном, 64 % при трёх; в тройке в 100 %.
3. higher=no — 48 % против 12 %; в тройке в 76 % замеров.

Первые два неразличимы между собой (0.093 ± 0.035 против 0.087 ± 0.021).
Четвёртый, guardian, превышает свой разброс, но в тройке лишь в 8 % замеров —
в состав тройки не входит.

absences почти не работает — это результат, а не ошибка: важность +0.0009 ± 0.0055,
12-е место из 39. При этом коэффициент логрегрессии у неё высокий (+0.392) —
связь есть, вклада нет.

## Что разошлось с исходным ТЗ (проверено на данных)
- `max_depth=6` НЕ делает хуже: средние 0.8339 / 0.8331 / 0.8334 для 6 / 8 /
  None. Глубина не значит почти ничего, решает `min_samples_leaf`.
- Удаление 15 студентов с G3==0 НЕ повышает метрику, а понижает:
  уровень 2 — 0.839 → 0.832, PR-AUC 0.499 → 0.462. Оставляем их и по делу,
  и по цифрам.

## Устройство репозитория
- `risk.ipynb` — основной ноутбук, 22 блока, проходит Restart & Run All.
- `src/scoring.py` — `prep`, `fit_blend`, `load_model`, `predict_risk`, `explain`.
  Ноутбук импортирует их, а не дублирует. Веб-интерфейс должен опираться
  на этот же модуль.
- `cache/*.pkl` — кэш тяжёлых блоков, ключ = хеш параметров + отпечаток данных.
  При смене параметров пересчитывается сам, удалять руками не нужно.
- `models/` — финальный бленд, обученный на всех 649 строках (блок 22).

## Запуск
Окружение — `.venv` (Python 3.11.9), зависимости в `requirements.txt`.
Первый прогон ноутбука ≈ 15–20 минут (сетка гиперпараметров), повторный — под минуту.

## Обозначение разброса
Печатаем `0.826 ± 0.043 (SE 0.010)`: ± — стандартное отклонение между фолдами,
SE — стандартная ошибка среднего. Не путать: первое говорит, насколько метрика
пляшет от разбиения к разбиению, второе — насколько точно измерено среднее.
